#include <stdio.h>

int main(){
    int a = 15;

    // Displaying numbers with Base prefix
    printf("Decimal: %d\n", a);
    printf("Octal: %#o\n", a);
    printf("Hexa decimal: %#x", a);

    return 0;
}